# macOS
.DS_Store

# Editor / IDE
.vscode/
.idea/

# Brainstorm session state (visual companion attempt)
.superpowers/

# Claude Code local agent state
.claude/

# Source spreadsheets (private working data; site reads from published Google Sheet)
*.xlsx

# Cowork plugin packages (binary deliverables, not website files)
*.plugin

# Node (not used today, but future-proof)
node_modules/

# Logs
*.log

# Playwright debug snapshots (scratch, not project files)
.playwright-mcp/

# Personal ID documents -- never let these near a public repo
*.jp2
KAD PENGENALAN*

# Private working files -- this repo is public AND served live on mytournamentpb.com
docs/rebuild-plan.md
admin-pb-hq-workflow.html
*.plugin.bak
